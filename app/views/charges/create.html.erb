<p id="notice"><%= notice %></p>
<div class="products">
  <h2>Thanks, you paid <%= number_to_currency( @amount / 100 ) %> amount!</h2>
</div>
<% session[:cart_id] = nil %>

<%= link_to 'Back Home', root_path %>